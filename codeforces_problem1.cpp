#include<bits/stdc++.h>
using namespace std;

int main(){
	int t;
	cin>>t;
	while(t--) {
	    /* code */
	
	    /* code */
	    int n,l,j;
	    cin>>n;
	    cout<<n;
	    j=n;
	    int a[j];
	    
	    for (int i = 0; i < n; ++i)
	    {
	    	/* code */

	    	cin >> a[i];


	    }
	    cout << a[n];

	   //  for (int i = 0; i < l; ++i)
	   //  {
	   //  	/* code */
	   //  	 int binaryNum[a[i]];
	   //  	 int j = 0;
    // while (n > 0) {

    //     // storing remainder in binary array
    //     binaryNum[j] = n % 2;
    //     n = n / 2;
    //     j++;
    // }

    // for (int z = j - 1; z >= 0; z--)
    //     cout << binaryNum[z];

	   //  }
	   


	}
}